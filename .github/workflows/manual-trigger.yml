name: Manual Trigger Workflow

on:
  workflow_dispatch:
    inputs:
      name:
        description: "Your name"
        required: true

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello, ${{ github.actor }}!"
